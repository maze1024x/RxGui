namespace ::

entry {
    ShowWindow({
        @use a = PlainButton('AAAA'),
        @use b = PlainButton('BBBB'),
        @use c = PlainButton('CCCC'),
        @use s = Splitter {
            content: List(a.Widget, b.Widget, c.Widget)
        },
        Window($('Splitter'), Row(s.Widget))
    })
}


