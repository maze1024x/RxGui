namespace ::

entry {
    ShowWindow({
        @use a = Label($('AAAAAAAAA')),
        @use b = TextBox('BBBB'),
        @use c = Label($('CCCC')),
        @use d = TextBox('DDDD'),
        Window($('Layout2'), Grid(
            Span(a.Widget, 0, 0),
            Span(b.Widget, 0, 1),
            Span(c.Widget, 1, 0, Right),
            Span(d.Widget, 1, 1)
        ))
    })
}


